import type { FC } from 'react';
import { Button } from 'zarm';

const App: FC = () => {
    return (
        <div>
            <Button theme="primary">aaaa</Button>
        </div>
    );
};

export default App;
